# This file is generated by the nukenpm release workflow. Do not edit by hand.
class Nukenpm < Formula
  desc "Interactive TUI to find and clean node_modules directories"
  homepage "https://github.com/preschian/nukenpm"
  version "${VERSION}"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/preschian/nukenpm/releases/download/v${VERSION}/nukenpm-${VERSION}-aarch64-apple-darwin.tar.gz"
      sha256 "${SHA_AARCH64_APPLE_DARWIN}"
    end
    on_intel do
      url "https://github.com/preschian/nukenpm/releases/download/v${VERSION}/nukenpm-${VERSION}-x86_64-apple-darwin.tar.gz"
      sha256 "${SHA_X86_64_APPLE_DARWIN}"
    end
  end

  on_linux do
    on_intel do
      url "https://github.com/preschian/nukenpm/releases/download/v${VERSION}/nukenpm-${VERSION}-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "${SHA_X86_64_UNKNOWN_LINUX_GNU}"
    end
  end

  def install
    bin.install "nukenpm"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/nukenpm --version")
  end
end
